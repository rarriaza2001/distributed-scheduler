LeadershipState {
  leaderId: string
  leaseToken: string
  acquiredAt: timestamp
  expiresAt: timestamp
  renewedAt: timestamp
}

Lives in Redis only

Purpose
- defines who the active scheduler is
- supports renewal and failover
- prevents stale leader ambiguity